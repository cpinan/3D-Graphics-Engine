// GAME LOGIC
class Vector {
	constructor(x, y) {
		this.x = x;
		this.y = y;
	};
};

function preload() {

}

function setup() {
	// createCanvas(WIDTH, HEIGHT);
}

function draw() {
	clear();
	background(0);
}

function keyPressed() {

}